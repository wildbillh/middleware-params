{
  "name": "middleware-params",
  "version": "1.0.6",
  "description": "Provides an easy mechanism for sending data to express middleware",
  "main": "index.js",
  "scripts": {
    "test": "node_modules/.bin/mocha --reporter spec --ui bdd test",
    "coverage": "node_modules/.bin/nyc --all --reporter=html --reporter=text node_modules/.bin/mocha --reporter spec --ui bdd test"
  },
  "keywords": [
    "express",
    "middleware",
    "params",
    "parameters"
  ],
  "author": "Bill Hodges",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.2.1",
    "nyc": "^14.1.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wildbillh/middleware-params.git"
  }
}
